// All times are milliseconds unless otherwise indicated.

export default {
  snackbarCloseTimeout: 2500,
  snackbarCloseAnimation: 400, // we don't actually set it here, we just know it's this amount
};
